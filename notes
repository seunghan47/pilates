@Qualifier
--> given multiple classes implementing same interface. allows spring to pick which ones
--> eg. constructor injection
--> eg. private final Instructor instructor
--> eg. public classScheduler(Qualifier('yogaInstructor') Instructor instructor) {
-->     this.instructor = instructor;
-->     }

@Primary
--> Picks the implementation when you use this qualifer.

@Lazy
--> Only starts up beans/components when they're actually needed
--> used to save time, data, optimization.

@Bean
--> this is like the dish. Spring instantiates an object. it's called Bean
--> make use of 3rd party class available to spring.

@Configuration
--> this is like the recipe. it tells spring HOW to configure something